# Online Shop

Учебный pet-проект интернет-магазина: каталог товаров с фильтрацией и
сортировкой, JWT-авторизация, личный кабинет, корзина, оформление заказа
и избранное.

## Стек технологий

**Backend:**
- FastAPI
- SQLAlchemy (SQLite)
- Pydantic — валидация данных
- JWT (python-jose) — авторизация по access/refresh токенам
- bcrypt — хеширование паролей

**Frontend:**
- React + TypeScript
- React Router — маршрутизация между страницами
- react-icons

## Функциональность

**Каталог**
- Список товаров с изображением, названием, ценой и категорией
- Поиск по названию, фильтр по категории и диапазону цен
- Сортировка по цене (возрастание/убывание)
- Кнопка сброса всех фильтров
- Отдельная страница товара с подробным описанием

**Авторизация**
- Регистрация и вход по email/паролю
- Валидация пароля (минимальная длина, буквы, цифры)
- JWT access и refresh токены
- Хранение сессии между перезагрузками страницы

**Личный кабинет**
- Просмотр email и даты регистрации
- Смена email и пароля (со сверкой текущего пароля)
- История оформленных заказов

**Корзина и заказы**
- Добавление/удаление товаров, изменение количества
- Оформление заказа как авторизованным, так и гостевым пользователем
- Хранение данных заказа (ФИО, телефон, email, адрес) и его позиций

**Избранное**
- Добавление/удаление товаров в избранное (только для авторизованных)
- Отдельная страница со списком избранных товаров

## Запуск проекта

### Backend

```bash
cd backend
python -m venv venv
venv\Scripts\activate       # Windows
# source venv/bin/activate  # macOS/Linux

pip install -r requirements.txt
```

Создайте файл `.env` в папке `backend` по образцу `.env.example` и укажите
`SECRET_KEY` (например, сгенерировав его командой):

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Запуск сервера:

```bash
uvicorn app.main:app --reload
```

Backend будет доступен на `http://localhost:8000`, документация API —
на `http://localhost:8000/docs`.

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:3000`.

## Структура проекта

```
online-shop/
├── backend/
│   └── app/
│       ├── main.py        — точка входа, эндпоинты товаров
│       ├── auth.py        — регистрация, логин, refresh, профиль
│       ├── orders.py      — оформление и история заказов
│       ├── favorites.py   — избранное
│       ├── models.py      — модели базы данных
│       ├── schemas.py     — схемы валидации Pydantic
│       ├── security.py    — хеширование паролей, JWT
│       └── config.py      — конфигурация из переменных окружения
└── frontend/
    └── src/
        ├── App/            — корневой компонент, маршрутизация
        ├── Entities/       — сущности (товар, пользователь) и работа с API
        ├── Widgets/        — переиспользуемые блоки интерфейса (шапка, модалки)
        └── Pages/          — страницы (профиль, товар, избранное, заказ)
```